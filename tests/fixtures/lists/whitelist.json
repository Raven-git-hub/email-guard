{
  "_note": "SYNTHETIC test fixture -- invented contacts only. This set satisfies the one-domain-one-list invariant, so it loads with validation on. The deliberately-conflicting `both@conflict.example` pair lives in ../lists-conflict/ instead.",
  "whitelist": [
    {
      "email": "friend@example.org",
      "friendly_name": "Trusted Friend",
      "tags": ["personal"],
      "known_structures": [
        {
          "name": "ALL EMAILS",
          "key_phrases": []
        }
      ]
    }
  ]
}
